import { createFileRoute } from "@tanstack/react-router";
import Page from "@/app/workspace/providers/routing-rules/page";

export const Route = createFileRoute("/workspace/providers/routing-rules/")({
	component: Page,
});
