import { createFileRoute } from "@tanstack/react-router";
import Page from "@/app/workspace/adaptive-routing/page";

export const Route = createFileRoute("/workspace/adaptive-routing/")({
	component: Page,
});
