import { createFileRoute } from "@tanstack/react-router";
import Page from "@/app/workspace/mcp-tool-groups/page";

export const Route = createFileRoute("/workspace/mcp-tool-groups/")({
	component: Page,
});
