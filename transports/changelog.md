[fix]: root HTTP span uses stable OTel semconv attributes (`http.request.method`, `url.path`, `http.route`, `http.response.status_code`, `user_agent.original`) [@adjohn](https://github.com/adjohn)
[fix]: root HTTP span name uses the request path without the query string [@adjohn](https://github.com/adjohn)
