- fix: a chat completion to a model whose catalog row is Responses-only is now served through the Responses API with the caller's reasoning effort intact instead of having its reasoning disabled. `dropUnsupportedParams` no longer applies the chat-completions-only "reasoning with tools" fallback (`reasoning_effort: "none"` or dropping reasoning) to a request already marked for `/v1/responses`, where reasoning and function tools are accepted together. Azure Foundry `gpt-6-astra` / `gpt-5.6-*` reject tools + `reasoning_effort` on `/v1/chat/completions`, and `gpt-6-astra` also rejects `reasoning_effort: "none"`; once the datasheet publishes those rows as Responses-first, every chat completion to them (with or without tools) takes the Responses path by default (#7275)
