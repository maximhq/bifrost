---
phase: 03-production-deployment
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/platform-services/bifrost/overlays/prod/kustomization.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/namespace.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/deployment.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/service.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/configmap.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/llm-providers-externalsecret.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/oidc-externalsecret.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/tailscale-ingress.yaml  # infra-ctrl repo (NEW)
  - apps/platform-services/bifrost/overlays/prod/network-policy.yaml  # infra-ctrl repo (NEW)
autonomous: true
requirements: [DEPLOY-02]

must_haves:
  truths:
    - "Prod overlay exists and kustomize build succeeds"
    - "Prod overlay references fork image, not upstream maximhq/bifrost"
    - "Prod OIDC issuer_url points to prod Keycloak, not dev"
    - "Prod Tailscale ingress hostname is bifrost-prod, not bifrost-dev"
    - "Prod labels use env: prod and cluster: prod-sks"
  artifacts:
    - path: "apps/platform-services/bifrost/overlays/prod/kustomization.yaml"
      provides: "Prod overlay Kustomization with env=prod, cluster=prod-sks"
      contains: "prod-sks"
    - path: "apps/platform-services/bifrost/overlays/prod/deployment.yaml"
      provides: "Prod deployment with fork image and OIDC env var"
      contains: "ghcr.io/stragix-innovations/bifrost"
    - path: "apps/platform-services/bifrost/overlays/prod/configmap.yaml"
      provides: "Prod ConfigMap with prod Keycloak issuer_url"
      contains: "keycloak-prod"
    - path: "apps/platform-services/bifrost/overlays/prod/oidc-externalsecret.yaml"
      provides: "Prod OIDC ExternalSecret"
      contains: "bifrost-oidc"
    - path: "apps/platform-services/bifrost/overlays/prod/tailscale-ingress.yaml"
      provides: "Prod Tailscale ingress with bifrost-prod hostname"
      contains: "bifrost-prod"
  key_links:
    - from: "apps/platform-services/bifrost/overlays/prod/deployment.yaml"
      to: "apps/platform-services/bifrost/overlays/prod/oidc-externalsecret.yaml"
      via: "secretKeyRef to bifrost-oidc"
      pattern: "bifrost-oidc"
    - from: "apps/platform-services/bifrost/overlays/prod/configmap.yaml"
      to: "Keycloak prod"
      via: "OIDC issuer_url"
      pattern: "keycloak-prod"
---

<objective>
Create the prod overlay for Bifrost in infra-ctrl by copying the dev overlay (created in Plan 01) and adjusting all environment-specific values: labels, cluster name, Keycloak URL, Tailscale hostname, and tags.

Purpose: Enable ArgoCD ApplicationSet to auto-discover the prod overlay and deploy Bifrost with OIDC to the production cluster.

Output: Complete prod overlay directory at `apps/platform-services/bifrost/overlays/prod/` with 9 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-deployment/03-CONTEXT.md
@.planning/phases/03-production-deployment/03-01-SUMMARY.md

<interfaces>
<!-- Dev overlay from Plan 01 is the source. Copy and adjust these env-specific values: -->

<!-- kustomization.yaml: env: dev -> env: prod, cluster: dev-sks -> cluster: prod-sks -->
<!-- configmap.yaml: keycloak-dev.tail15b586.ts.net -> keycloak-prod.tail15b586.ts.net -->
<!-- tailscale-ingress.yaml: bifrost-dev -> bifrost-prod, tag:dev -> tag:prod -->
<!-- All other files: identical to dev (namespace, service, deployment, externalsecrets, network-policy) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prod overlay by copying dev and adjusting environment-specific values</name>
  <files>
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/kustomization.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/namespace.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/deployment.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/service.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/configmap.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/llm-providers-externalsecret.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/oidc-externalsecret.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/tailscale-ingress.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/prod/network-policy.yaml
  </files>
  <read_first>
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/kustomization.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/deployment.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/configmap.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/namespace.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/service.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/llm-providers-externalsecret.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/oidc-externalsecret.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/tailscale-ingress.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl/apps/platform-services/bifrost/overlays/dev/network-policy.yaml
    /Users/shawnwalker/code/stragix/infra-ctrl-bifrost-eval/apps/platform-services/bifrost/overlays/prod/configmap.yaml
  </read_first>
  <action>
    **IMPORTANT: All file paths are in the infra-ctrl repo at `/Users/shawnwalker/code/stragix/infra-ctrl/`.**

    Create the directory `apps/platform-services/bifrost/overlays/prod/` and populate it by copying every file from the dev overlay (as updated by Plan 01). Then make these environment-specific changes:

    **Files that are IDENTICAL to dev (copy verbatim, no changes):**
    - `namespace.yaml` -- same namespace `bifrost-system` in all envs
    - `service.yaml` -- same ClusterIP service in all envs
    - `deployment.yaml` -- same image, env vars, probes, security context in all envs
    - `llm-providers-externalsecret.yaml` -- same Vault paths for LLM API keys in all envs
    - `oidc-externalsecret.yaml` -- same Vault path for bifrost OIDC secret in all envs
    - `network-policy.yaml` -- same egress/ingress rules in all envs

    **Files with environment-specific changes:**

    1. **kustomization.yaml** -- Change labels only:
       - `env: dev` -> `env: prod`
       - `cluster: dev-sks` -> `cluster: prod-sks`
       - Everything else identical (same resources list, same namespace)

    2. **configmap.yaml** -- Change OIDC issuer_url only:
       - `"issuer_url": "https://keycloak-dev.tail15b586.ts.net/realms/stragixlabs"` -> `"issuer_url": "https://keycloak-prod.tail15b586.ts.net/realms/stragixlabs"`
       - Everything else identical (same providers, same client_id, same scopes)

    3. **tailscale-ingress.yaml** -- Change hostname and tag:
       - `tailscale.com/tags: "tag:dev"` -> `tailscale.com/tags: "tag:prod"`
       - `host: bifrost-dev` -> `host: bifrost-prod` (in rules and tls sections)
       - Everything else identical (same ingressClassName, same backend service)
  </action>
  <verify>
    <automated>cd /Users/shawnwalker/code/stragix/infra-ctrl && kustomize build --enable-helm apps/platform-services/bifrost/overlays/prod 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - 9 files exist in `apps/platform-services/bifrost/overlays/prod/`
    - kustomization.yaml contains `env: prod` and `cluster: prod-sks`
    - configmap.yaml contains `keycloak-prod.tail15b586.ts.net` (NOT keycloak-dev)
    - configmap.yaml contains `"client_id": "bifrost"` in oidc section
    - deployment.yaml contains `ghcr.io/stragix-innovations/bifrost:main` (NOT maximhq/bifrost)
    - deployment.yaml contains `BIFROST_OIDC_CLIENT_SECRET` env var
    - tailscale-ingress.yaml contains `host: bifrost-prod` and `tag:prod`
    - oidc-externalsecret.yaml contains `infrastructure/auth/keycloak/clients/bifrost`
    - `kustomize build --enable-helm apps/platform-services/bifrost/overlays/prod` succeeds
    - No remaining references to `dev-sks` or `keycloak-dev` or `bifrost-dev` in prod overlay files
  </acceptance_criteria>
  <done>
    Prod overlay exists at `apps/platform-services/bifrost/overlays/prod/` with all 9 manifest files. Environment-specific values (labels, Keycloak URL, Tailscale hostname) are correct for prod. Kustomize build validates successfully. ArgoCD ApplicationSet will auto-discover this overlay.
  </done>
</task>

</tasks>

<verification>
1. `kustomize build --enable-helm apps/platform-services/bifrost/overlays/prod` produces valid YAML without errors
2. `grep -r "dev-sks\|keycloak-dev\|bifrost-dev\|tag:dev" apps/platform-services/bifrost/overlays/prod/` returns no results
3. `grep -r "prod-sks" apps/platform-services/bifrost/overlays/prod/kustomization.yaml` returns a match
4. `grep -r "keycloak-prod" apps/platform-services/bifrost/overlays/prod/configmap.yaml` returns a match
5. Both dev AND prod overlays build successfully: `for env in dev prod; do kustomize build --enable-helm apps/platform-services/bifrost/overlays/$env >/dev/null && echo "OK: $env"; done`
</verification>

<success_criteria>
- Prod overlay directory exists with 9 files
- Both dev and prod overlays pass kustomize build
- No dev-specific values leaked into prod overlay
- Prod overlay has correct environment labels, Keycloak URL, and Tailscale hostname
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-deployment/03-02-SUMMARY.md`
</output>
