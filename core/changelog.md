- [fix]: Bedrock InvokeModel streams keep safeguard_results (Claude Code auto-mode classifier). Bedrock delivers it nested in message_delta.delta rather than at the event's top level, and AnthropicStreamDelta had no field for it, so the verdicts were dropped on every streamed turn and Claude Code fell back to its own billed classifier requests ("this session isn't eligible"). The nested value now rides the message_delta chunk and is re-emitted in the same position [@redcapcloud](https://github.com/redcapcloud)
