- fix: give a Bedrock message a placeholder text block instead of a null `content` field when it has no text and no tool calls - `BedrockMessage.Content` has no `omitempty`, so a message with empty text and no tool calls (or an empty `tool_calls` array) serialized as `content:null`, which Converse rejects with "Member must not be null" (#2765)
- [fix]: marshal required nullable response fields as null [@PSR94](https://github.com/PSR94)
- fix: accept top-level arrays from OpenAI-compatible model APIs [@dani29](https://github.com/dani29)
- feat: add Baseten to Hugging Face inference provider discovery [@nicolastoulemont](https://github.com/nicolastoulemont)
- fix: strip the encrypted reasoning signature when the upstream reports the field as unsupported (e.g. Bedrock Converse replaying a Claude signature onto a non-Anthropic model after a mid-conversation model switch), extending the existing unverifiable-signature fail-soft
- fix: clear Anthropic raw-body passthrough based on the resolved provider and model pair, so non-Claude models on multi-family providers (Vertex, Azure, Bedrock Mantle) convert the request instead of passing the Anthropic payload through
- feat: add native `x-bf-request-timeout` header for a total request/stream deadline, independent of the `x-bf-stream-idle-timeout` (#4805) -- arms once per call across every fallback attempt, sets `BifrostContextKeyRequestTimeoutFired` before cancelling so plugins can distinguish it from an arbitrary cancellation [@public-aanp-tanium](https://github.com/public-aanp-tanium)
- fix: make `wrapStreamForRequestTimeout`'s forwarding send select on `ctx.Done()` so a consumer that abandons the stream can't block it forever and skip its deferred cleanup [@public-aanp-tanium](https://github.com/public-aanp-tanium)
- fix: derive a request-scoped context for nil-ctx `handleRequest`/`handleStreamRequest` callers before arming the request timeout, so a fired deadline cancels only that request instead of the shared root every nil-ctx caller falls back to [@public-aanp-tanium](https://github.com/public-aanp-tanium)
- fix: skip `wrapStreamForRequestTimeout`'s proxy channel and goroutine entirely when no request timeout is armed, and check `ctx.Done()` before each forwarding send so a chunk can't be forwarded in the same instant cancellation fires [@public-aanp-tanium](https://github.com/public-aanp-tanium)
- fix: derive the nil-ctx request context only when a request timeout is actually declared, and release it once the request (or the fully drained stream) completes, so a nil-ctx caller no longer leaves a `watchCancellation` goroutine blocked on the shared root until shutdown [@public-aanp-tanium](https://github.com/public-aanp-tanium)
